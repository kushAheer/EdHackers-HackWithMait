@* @model Subjects
@{
    ViewData["Title"] = "Add Subject";
}

<div class="container-fluid">
    <h1>Add Subject</h1>
    <form class="row g-3" asp-action="addSubject">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="mb-3">
            <label asp-for="SubjectName" class="control-label">Subject Name</label>
            <input type="text" asp-for="SubjectName" class="form-control" />
            <span asp-validation-for="SubjectName" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="SubjectDesc" class="control-label">Subject Description</label>
            <input type="text" asp-for="SubjectDesc" class="form-control" />
            <span asp-validation-for="SubjectDesc" class="text-danger"></span>
        </div>
        <div class="form-group">
            <input type="submit" value="addSubject" class="btn btn-outline-success float-md-end" />
            <a class="btn btn-outline-secondary" asp-action="Index">Show All</a>
        </div>
    </form>


</div>
 *@